<resources>

    <!-- Base application theme. Estilo sin ActionBar-->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Main application theme. Estilo con ActionBar -->
    <style name="AppThemeMain" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <!-- Estilo para que no se muestre una pantalla en blanco antes del splash -->
    <style name="BeforeSplash" parent="AppTheme">
    <item name="android:windowBackground">@drawable/gradient</item>
    </style>

    <!-- Estilo para las cajas de texto -->
    <style name="MyEditTextTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColorHint">@color/colorPrimaryDark</item>
        <item name="android:textSize">17sp</item>
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorControlHighlight">@android:color/white</item>
    </style>

    <!-- Estilo para las imagenes a la izquierda de las cajas de texto -->
    <style name="ImagenesLogin">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:scaleType">fitXY</item>
    </style>

    <!-- Estilo para textos Login -->
    <style name="TextosLogin">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowRadius">5</item>
    </style>

    <!-- Estilo para imagenes logo Splash -->
    <style name="SplashLogo">
        <item name="android:layout_width">153dp</item>
        <item name="android:layout_height">138dp</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_marginTop">34dp</item>
        <item name="android:layout_alignParentTop">true</item>
    </style>


</resources>
